{
  "name": "pokedao",
  "private": true,
  "version": "0.0.0",
  "description": "PokeDAO monorepo",
  "workspaces": [
    "api",
    "bot",
    "worker",
    "ml"
  ],
  "scripts": {
    "build:scripts": "tsc -p ./scripts/tsconfig.json",
    "build": "pnpm -w -r --filter ./... run build || true",
    "dev": "echo \"run package-level dev scripts\""
  },
  "devDependencies": {
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0"
  },
  "dependencies": {
    "typescript": "^5.9.2"
  }
}
