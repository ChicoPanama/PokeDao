{
  "name": "pokedao",
  "private": true,
  "version": "0.0.0",
  "description": "PokeDAO monorepo",
  "workspaces": [
    "api",
    "bot",
    "worker",
    "ml"
  ],
  "scripts": {
    "build:scripts": "tsc -p ./scripts/tsconfig.json",
    "build": "pnpm run build:scripts",
    "dev": "echo \"run package-level dev scripts\""
  },
  "prisma": {
    "seed": "npx --yes ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "@prisma/client": "^6.15.0",
    "@types/node": "^24.3.1",
    "dotenv": "^17.2.2",
    "prisma": "^6.15.0"
  },
  "dependencies": {
    "redis": "^5.8.2",
    "typescript": "^5.9.2"
  }
}
